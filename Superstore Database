SELECT *
FROM superstore
ORDER BY price;

SELECT MAX (stock_quantity)
FROM superstore;

SELECT MIN (price), "Kitchen Supplies"
FROM superstore;

SELECT MIN (average_rating), "Electronics"
FROM superstore;
